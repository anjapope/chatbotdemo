# Copy this file to .env and put your real secret values there.
# Do NOT commit your real .env to version control.

# OpenAI API key — required to proxy to OpenAI and get original replies
# Example: OPENAI_API_KEY=sk-...
OPENAI_API_KEY=

# Optional: model to request (defaults to gpt-3.5-turbo if not set)
OPENAI_MODEL=gpt-4o

# Optional: admin token for admin endpoints
ADMIN_TOKEN=secret-token

# Optional: how many candidates to request for reranking (default 5)
RERANK_N=5
